# Show Employee Name in Employee Sync Status

- Keep `Employee Code` as the primary visible identifier.
- Show `Employee Name` next to `Employee Code` in Employee Sync Status list view.
- Show `Employee Name` next to `Employee Code` in the Controller form Employees Synced tab.
- Keep the hidden `Employee` Many2one field for traceability.
- No sync, cron, attendance, or direction logic was changed.
