# Employees API identifier correction

Current rule:
- `employee_id` is `hr.employee.id`.
- `pin` is the device password/PIN.
- The older idea of returning device identifier from PIN/barcode has been removed.
