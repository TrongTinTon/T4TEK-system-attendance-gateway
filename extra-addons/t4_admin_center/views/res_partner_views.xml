<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- ==========================================
             LIST VIEW: Company
             ========================================== -->
        <record id="view_res_partner_tree_admin_center" model="ir.ui.view">
            <field name="name">res.partner.list.admin_center</field>
            <field name="model">res.partner</field>
            <field name="priority">1</field>
            <field name="arch" type="xml">
                <list string="Danh Sách Công Ty">
                    <field name="name" string="Tên công ty"/>
                    <field name="email" string="Email"/>
                    <field name="phone" string="Điện thoại"/>
                    <field name="website" string="Website"/>
                    <field name="city" string="Thành phố"/>
                    <field name="country_id" string="Quốc gia"/>
                </list>
            </field>
        </record>

        <!-- ==========================================
             FORM VIEW: Company
             ========================================== -->
        <record id="view_res_partner_form_admin_center" model="ir.ui.view">
            <field name="name">res.partner.form.admin_center</field>
            <field name="model">res.partner</field>
            <field name="priority">1</field>
            <field name="arch" type="xml">
                <form string="Thông Tin Công Ty">
                    <sheet>
                        <div class="oe_button_box" name="button_box"/>
                        <field name="avatar_128" invisible="1"/>
                        <field name="image_1920" widget="image" class="oe_avatar" options="{'preview_image': 'avatar_128'}"/>

                        <div class="oe_title mb24">
                            <field name="is_company" invisible="1"/>
                            <field name="commercial_partner_id" invisible="1"/>
                            <field name="active" invisible="1"/>
                            <label for="name" class="oe_edit_only" string="Tên Công Ty"/>
                            <h1>
                                <field name="name" placeholder="Tên công ty..."/>
                            </h1>
                        </div>
                        <group>
                            <group string="Địa Chỉ">
                                <label for="street" string="Địa chỉ"/>
                                <div class="o_address_format">
                                    <field name="street" placeholder="Đường..." class="o_address_street"/>
                                    <field name="street2" placeholder="Đường 2..." class="o_address_street"/>
                                    <field name="city" placeholder="Thành phố" class="o_address_city"/>
                                    <field name="state_id" class="o_address_state" placeholder="Tỉnh/Thành" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'zip': zip}"/>
                                    <field name="zip" placeholder="Mã bưu chính" class="o_address_zip"/>
                                    <field name="country_id" placeholder="Quốc gia" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                                </div>
                                <field name="vat" string="Mã số thuế" placeholder="e.g. BE0477472701"/>
                            </group>
                            <group string="Liên Lạc">
                                <field name="phone" string="Điện thoại" widget="phone"/>
                                <field name="email" string="Email" widget="email"/>
                                <field name="website" string="Website" widget="url" placeholder="e.g. https://www.odoo.com"/>
                                <field name="category_id" widget="many2many_tags" options="{'color_field': 'color', 'no_create_edit': True}" placeholder="Nhãn..."/>
                            </group>
                        </group>

                        <notebook>

                            <page string="Danh sách khu vực" name="contacts_addresses">
                                <field name="child_ids" mode="kanban" string="Danh sách khu vực" context="{'default_parent_id': id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_lang': lang, 'default_user_id': user_id, 'default_type': 'order'}">
                                    <kanban>
                                        <field name="id"/>
                                        <field name="name"/>
                                        <field name="type"/>
                                        <field name="street"/>
                                        <field name="street2"/>
                                        <field name="city"/>
                                        <field name="state_id"/>
                                        <field name="zip"/>
                                        <field name="country_id"/>
                                        <field name="comment"/>
                                        <templates>
                                            <t t-name="card">
                                                <div class="o_kanban_record_details p-2">
                                                    <div class="o_kanban_record_top">
                                                        <div class="o_kanban_record_headings">
                                                            <strong class="o_kanban_record_title fs-5">
                                                                <field name="name"/>
                                                            </strong>
                                                        </div>
                                                    </div>
                                                    <div class="o_kanban_record_body mt-2">
                                                        <div t-if="record.street.raw_value or record.city.raw_value" class="d-flex align-items-baseline">
                                                            <i class="fa fa-map-marker me-2 text-primary" title="Địa chỉ"/>
                                                            <span>
                                                                <field name="street"/>
                                                                <t t-if="record.street2.raw_value">, <field name="street2"/></t>
                                                                <t t-if="record.city.raw_value"> <field name="city"/></t>
                                                                <t t-if="record.state_id.raw_value">, <field name="state_id"/></t>
                                                            </span>
                                                        </div>
                                                        <div t-if="record.comment.raw_value" class="text-muted small mt-2 d-flex align-items-center">
                                                            <i class="fa fa-info-circle me-2"/>
                                                            <field name="comment"/>
                                                        </div>
                                                    </div>
                                                </div>
                                            </t>
                                        </templates>
                                    </kanban>
                                    <form string="Khu vực">
                                        <sheet>
                                            <field name="type" widget="radio" options="{'horizontal': true}" invisible="1"/>
                                            <field name="parent_id" invisible="1"/>
                                            <group>
                                                <group>
                                                    <field name="name" string="Tên khu vực" placeholder="e.g. Kho Quận 1" required="1"/>
                                                </group>
                                                <group>
                                                    <div invisible="type == 'contact'" class="o_address_format">
                                                        <field name="street" placeholder="Đường..." class="o_address_street"/>
                                                        <field name="street2" placeholder="Đường 2..." class="o_address_street"/>
                                                        <field name="city" placeholder="Thành phố" class="o_address_city"/>
                                                        <field name="state_id" class="o_address_state" placeholder="Tỉnh/Thành" options="{'no_open': True, 'no_quick_create': True}" context="{'country_id': country_id, 'zip': zip}"/>
                                                        <field name="zip" placeholder="Mã bưu chính" class="o_address_zip"/>
                                                        <field name="country_id" placeholder="Quốc gia" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                                                    </div>
                                                </group>
                                            </group>
                                            <field name="comment" placeholder="Ghi chú nội bộ..."/>
                                        </sheet>
                                    </form>
                                </field>
                            </page>

                            <page string="Cài Đặt" name="settings">
                                <group>
                                    <group string="Mail">
                                        <field name="x_disable_transactional_mail" widget="boolean_toggle"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- ==========================================
             ACTION: Chỉnh sửa Thông Tin Công Ty
             ========================================== -->
        <record id="action_res_company_admin_center" model="ir.actions.act_window">
            <field name="name">Thông Tin Công Ty</field>
            <field name="res_model">res.partner</field>
            <field name="view_mode">list,form</field>
            <field name="domain">['|', ('is_company', '=', True), ('partner_code', '!=', False), ('ref_company_ids', 'in', allowed_company_ids)]</field>
            <field name="context">{'default_is_company': True, 'delete': 0, 'create': 0}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Không tìm thấy thông tin công ty
                </p>
            </field>
        </record>

        <record id="action_res_company_admin_center_view_list" model="ir.actions.act_window.view">
            <field name="sequence" eval="10"/>
            <field name="view_mode">list</field>
            <field name="view_id" ref="view_res_partner_tree_admin_center"/>
            <field name="act_window_id" ref="action_res_company_admin_center"/>
        </record>

        <record id="action_res_company_admin_center_view_form" model="ir.actions.act_window.view">
            <field name="sequence" eval="20"/>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_res_partner_form_admin_center"/>
            <field name="act_window_id" ref="action_res_company_admin_center"/>
        </record>
    </data>
</odoo>
